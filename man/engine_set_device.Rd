% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{engine_set_device}
\alias{engine_set_device}
\title{Set the compute device for a BioclimEngine instance}
\usage{
engine_set_device(xptr, device)
}
\arguments{
\item{xptr}{External pointer returned by \code{\link{engine_create}}.}

\item{device}{Character scalar: one of \code{"auto"}, \code{"cpu"},
or \code{"gpu"}.}
}
\value{
\code{NULL} invisibly.
}
\description{
Controls whether the computation runs on a CUDA GPU or the CPU.
When \code{"auto"} is selected the engine uses the GPU if at least one
CUDA device is present, otherwise it falls back to the CPU.  GPU
requests on systems without a CUDA device silently fall back to the CPU.
}
\examples{
\dontrun{
ptr <- engine_create()
engine_set_device(ptr, "cpu")
}
}
\seealso{
\code{\link{engine_create}}, \code{\link{has_cuda}},
\code{\link{bioclim_engine}}
}
