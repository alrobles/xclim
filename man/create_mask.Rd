% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask.R
\name{create_mask}
\alias{create_mask}
\title{Create a binary mask raster from a polygon source}
\usage{
create_mask(
  polygon,
  reference_raster = NULL,
  output = tempfile(fileext = ".tif"),
  apply_to = NULL,
  apply_output = tempfile(fileext = ".tif")
)
}
\arguments{
\item{polygon}{Polygon source: an \code{sf} object, a character file path to an
OGR-readable vector source, or a \code{terra::SpatRaster} used directly as
a binary mask.}

\item{reference_raster}{Character string: path to a GDAL-readable raster
used to set the spatial reference (extent, resolution, CRS) of the
output mask.  Ignored when \code{polygon} is a \code{SpatRaster}.}

\item{output}{Character string: file path for the output mask GeoTIFF.
Defaults to a temporary file.}

\item{apply_to}{Character string or \code{NULL}: if supplied, path to a
GDAL-readable raster to which the mask will be applied.  Pixels outside
all polygons are set to \code{NA} in the result.}

\item{apply_output}{Character string: file path for the masked output
raster.  Used only when \code{apply_to} is not \code{NULL}.  Defaults to a
temporary file.}
}
\value{
When \code{apply_to} is \code{NULL}, returns the path to the binary mask
GeoTIFF invisibly.  When \code{apply_to} is provided, returns the path to the
masked output raster invisibly.
}
\description{
Converts a polygon source to a binary raster mask aligned to a reference
raster, and optionally applies that mask to an input raster (replacing
pixels outside all polygons with \code{NA}).
}
\details{
The function accepts three types of polygon input:
\describe{
\item{\code{sf} object}{Written to a temporary GeoJSON file via
\code{\link[sf:st_write]{sf::st_write()}} and then rasterized.
Requires the \pkg{sf} package.}
\item{Character file path}{Passed directly to the C++ rasterizer.
Any OGR-readable format is supported (shapefile, GeoJSON,
GeoPackage, etc.).}
\item{\code{SpatRaster} object}{Used directly as a pre-made mask raster.
Must already be binary (0/1) and aligned to \code{reference_raster}.
Requires the \pkg{terra} package.}
}

All GDAL-dependent operations skip gracefully (returning \code{NULL} invisibly
with a message) when the package was built without GDAL support.
}
\examples{
\donttest{
# Requires GDAL support at build time.
ref  <- system.file("extdata", "tiny.tif", package = "xclim")
poly <- tempfile(fileext = ".geojson")
writeLines(
  paste0(
    '{"type":"FeatureCollection","features":[{"type":"Feature",',
    '"geometry":{"type":"Polygon",',
    '"coordinates":[[[0,0],[1,0],[1,1],[0,1],[0,0]]]},',
    '"properties":{}}]}'
  ),
  poly
)
mask_path <- create_mask(poly, ref)
}
}
\seealso{
\code{\link[=rasterize_mask_cpp]{rasterize_mask_cpp()}}, \code{\link[=apply_mask_cpp]{apply_mask_cpp()}}
}
