% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/messages.R
\name{xclim-messages}
\alias{xclim-messages}
\title{Error and Warning Message Store}
\description{
xclim mirrors the \code{SpatMessages} pattern from the terra package to
provide a clean mechanism for propagating error and warning messages across
the C++/R boundary.

\strong{How the cross-boundary workflow works:}
\enumerate{
\item A C++ routine performs its computation and, instead of throwing directly,
records any error or warning strings into a session-level message store.
\item After every \code{.Call()} invocation, the R-side wrapper calls
\code{\link[=check_messages]{check_messages()}} to inspect the store and re-raise any messages as
native R conditions (\code{stop()} for errors, \code{warning()} for warnings).
\item Users can also inspect the store directly with \code{\link[=bioclim_errors]{bioclim_errors()}} and
\code{\link[=bioclim_warnings]{bioclim_warnings()}} before R conditions are raised, or clear it with
\code{\link[=clear_messages]{clear_messages()}}.
}

\strong{Functions available to users:}
\itemize{
\item \code{\link[=bioclim_errors]{bioclim_errors()}} – retrieve stored error messages.
\item \code{\link[=bioclim_warnings]{bioclim_warnings()}} – retrieve stored warning messages.
\item \code{\link[=has_error]{has_error()}} – \code{TRUE} if at least one error is stored.
\item \code{\link[=has_warning]{has_warning()}} – \code{TRUE} if at least one warning is stored.
\item \code{\link[=clear_messages]{clear_messages()}} – discard all stored messages.
}

\strong{Functions used internally (and by future C++ glue code):}
\itemize{
\item \code{push_error()} – store an error message.
\item \code{push_warning()} – store a warning message.
\item \code{check_messages()} – raise stored messages as R conditions and clear them.
}
}
